diff --git a/modules/views_filters_summary_verf/views_filters_summary_verf.module b/modules/views_filters_summary_verf/views_filters_summary_verf.module
index 4546147..a9aedd0 100644
--- a/modules/views_filters_summary_verf/views_filters_summary_verf.module
+++ b/modules/views_filters_summary_verf/views_filters_summary_verf.module
@@ -11,18 +11,13 @@
 function views_filters_summary_verf_views_filters_summary_info_alter(&$info, $filter) {
   if ($filter->getPluginId() === 'verf') {
     $values = [];
-    $configuration = $filter->configuration;
-    $target_entity_type_id = $configuration['verf_target_entity_type_id'];
-    $storage = \Drupal::entityTypeManager()->getStorage($target_entity_type_id);
-    foreach ($filter->value as $index => $index_value) {
-      if (empty($index_value)) {
-        continue;
-      }
+    $storage = \Drupal::entityTypeManager()->getStorage('node');
+    foreach ($filter->value as $index ) {
       if ($referenced_entity = $storage->load($index)) {
         $values[] = [
           'id' => $index,
           'raw' => $referenced_entity->id(),
-          'value' => $referenced_entity->label(),
+          'value' => $referenced_entity->title->value,
         ];
       }
     }
